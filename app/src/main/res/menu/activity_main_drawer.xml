<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_todo"
            android:icon="@drawable/todo"
            android:title="@string/todotask"
            android:checked="true"/>
        <item
            android:id="@+id/nav_done"
            android:icon="@drawable/done"
            android:title="@string/donetask" />
    </group>

    <item android:title="@string/tag">
        <menu>
            <item
                android:id="@+id/tag_work"
                android:title="@string/tag_work" />
            <item
                android:id="@+id/tag_learn"
                android:title="@string/tag_learn" />
            <item
                android:id="@+id/tag_live"
                android:title="@string/tag_live" />
        </menu>
    </item>

</menu>
